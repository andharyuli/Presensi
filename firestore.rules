rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Siswa, Kelas, Profil Sekolah, Presensi, Notifikasi, dan Template
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
